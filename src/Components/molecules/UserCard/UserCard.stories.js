import UserCard from './UserCard.js';

export default {
    title: 'Components/molecules/UserCard',
    Component: UserCard,
}

const Template = (args) => <UserCard />

export const Default = Template.bind({});
Default.args = {
}
